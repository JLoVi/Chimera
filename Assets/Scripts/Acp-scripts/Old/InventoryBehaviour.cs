using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Acp.Items
{
   /* public class InventoryBehaviour : MonoBehaviour, IItemContainer
    {
        [SerializeField] private Inventory inventory = null;

        private void Start()
        {
        }

        public BuildingSlot AddItem(BuildingSlot itemSlot) => inventory.AddItem(itemSlot);


        public int GetTotalQuantity(InventoryItem item) => inventory.GetTotalQuantity(item);
        

        public bool HasItem(InventoryItem item)
        {
            return inventory.HasItem(item);
        }

        public void RemoveAt(int slotIndex)
        {
            inventory.RemoveAt(slotIndex);
        }

        public void RemoveItem(BuildingSlot itemSlot)
        {
            inventory.RemoveItem(itemSlot);
        }

        
    }*/
}
